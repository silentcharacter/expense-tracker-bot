/** History page — scrollable, filterable transaction list. */

export function HistoryPage() {
  return (
    <div className="py-4">
      <p
        className="text-sm"
        style={{ color: "var(--app-text-secondary)" }}
      >
        Transaction history coming soon — your expenses will appear here.
      </p>
    </div>
  );
}
