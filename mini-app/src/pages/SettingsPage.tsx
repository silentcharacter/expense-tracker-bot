/** Settings page — profile, currencies, budgets, data export. */

export function SettingsPage() {
  return (
    <div className="py-4">
      <p
        className="text-sm"
        style={{ color: "var(--app-text-secondary)" }}
      >
        Settings coming soon — profile, currencies, and budgets will appear here.
      </p>
    </div>
  );
}
